[user]
	name = Jeremy Warner
	email = jeremy.warner@autodesk.com
[github]
    user = jeremywrnr
[credential]
	helper = "osxkeychain "
[alias]
    s= status
    a= add -A
    c= commit -am
    d= diff
    t= tag -n9
    lp= log -p
    lg= log --oneline --decorate --color --graph
    la= log --graph --decorate --pretty=format:'%C(yellow)%h %Cred%cr %Cblue(%an)%C(white)%d%Creset %s' --abbrev-commit --all
    lol = log --graph --decorate --pretty=oneline --abbrev-commit
    lola = log --graph --decorate --pretty=oneline --abbrev-commit --all
    dt= difftool
    br= branch
    co= checkout
    ps= push
    pl= pull
    open = !open-remote
    last= log -p --max-count=1 --word-diff
    alias= config --get-regexp alias
[core]
    excludesFile = /Users/jwrnr/.gitignore
[push]
    default = current
[diff]
    algorithm = minimal
    compactionHeuristic = true
    renames = true
[color]
    branch = auto
    diff = auto
    interactive = auto
    status = auto
    ui = true
[color "diff"]
    old = red bold
    new = green bold
    frag = magenta bold
    whitespace = red reverse
[color "diff-highlight"]
    oldNormal = red bold
    oldHighlight = "red bold 52"
    newNormal = "green bold"
    newHighlight = "green bold 22"
